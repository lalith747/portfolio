document.addEventListener('DOMContentLoaded',()=>{if(innerWidth<760)return;const c=document.createElement('div');c.className='quest-cursor';document.body.append(c);addEventListener('pointermove',e=>{c.style.left=e.clientX+'px';c.style.top=e.clientY+'px'});addEventListener('pointerdown',()=>c.classList.add('click'));addEventListener('pointerup',()=>c.classList.remove('click'));document.querySelectorAll('a,button').forEach(el=>{el.onmouseenter=()=>c.classList.add('active');el.onmouseleave=()=>c.classList.remove('active')})});
