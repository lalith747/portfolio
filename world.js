document.addEventListener('DOMContentLoaded',()=>{const c=document.querySelector('#skillSky');if(!c)return;const x=c.getContext('2d'),pts=['PYTHON','R','SQL','LINUX','SNAKEMAKE','HPC','ML','FULL-STACK','PRODUCT','BIOLOGY'].map((n,i)=>({n,x:80+Math.random()*innerWidth*.8,y:110+Math.random()*420,vx:(Math.random()-.5)*.25,vy:(Math.random()-.5)*.25}));let m={x:-999,y:-999};addEventListener('pointermove',e=>m={x:e.clientX,y:e.clientY});function draw(){const r=c.getBoundingClientRect(),d=devicePixelRatio;c.width=r.width*d;c.height=r.height*d;x.setTransform(d,0,0,d,0,0);x.clearRect(0,0,r.width,r.height);pts.forEach(p=>{p.x+=p.vx;p.y+=p.vy;if(p.x<40||p.x>r.width-40)p.vx*=-1;if(p.y<40||p.y>r.height-40)p.vy*=-1;pts.forEach(q=>{if(q===p)return;const z=Math.hypot(p.x-q.x,p.y-q.y);if(z<210){x.strokeStyle=`rgba(214,170,105,${.22-z/1200})`;x.beginPath();x.moveTo(p.x,p.y);x.lineTo(q.x,q.y);x.stroke()}});const near=Math.hypot(m.x-p.x,m.y-p.y)<100;x.fillStyle=near?'#a55742':'#d6aa69';x.beginPath();x.arc(p.x,p.y,near?6:3,0,7);x.fill();x.fillStyle='#eee4d1';x.font='10px DM Mono';x.fillText(p.n,p.x+9,p.y-8)});requestAnimationFrame(draw)}draw();let audio,playing=false;const toggle=document.querySelector('#soundToggle');toggle?.addEventListener('click',()=>{if(!audio){audio=new AudioContext()}playing=!playing;toggle.textContent=playing?'◉ SOUND: ON':'◌ SOUND: OFF';if(!playing)return;const notes=[110,146.8,164.8,130.8],start=audio.currentTime;notes.forEach((f,i)=>{const o=audio.createOscillator(),g=audio.createGain();o.type='sine';o.frequency.value=f;g.gain.setValueAtTime(.0001,start+i*.22);g.gain.exponentialRampToValueAtTime(.055,start+i*.22+.04);g.gain.exponentialRampToValueAtTime(.0001,start+i*.22+.4);o.connect(g).connect(audio.destination);o.start(start+i*.22);o.stop(start+i*.22+.45)})})});
