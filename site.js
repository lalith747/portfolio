document.addEventListener('DOMContentLoaded',()=>{const targets=document.querySelectorAll('.project-feature,.mission-list article,.timeline>div,.stat-row,.contact-card');targets.forEach(el=>el.classList.add('reveal'));const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.15});targets.forEach(el=>observer.observe(el));const hero=document.querySelector('.hero');if(hero){hero.addEventListener('pointermove',event=>{const box=hero.getBoundingClientRect();hero.style.setProperty('--mouse-x',`${(event.clientX-box.left-box.width/2)*.018}px`);hero.style.setProperty('--mouse-y',`${(event.clientY-box.top-box.height/2)*.018}px`)})}document.addEventListener('pointerdown',event=>{if(event.pointerType==='touch')return;for(let i=0;i<7;i++){const spark=document.createElement('i');spark.className='energy-spark';spark.style.left=`${event.clientX}px`;spark.style.top=`${event.clientY}px`;spark.style.setProperty('--dx',`${(Math.random()-.5)*95}px`);spark.style.setProperty('--dy',`${(Math.random()-.5)*95}px`);document.body.appendChild(spark);setTimeout(()=>spark.remove(),700)}})});
