document.addEventListener('DOMContentLoaded',()=>{const hero=document.querySelector('.opening');if(!hero)return;const canvas=document.createElement('canvas');canvas.className='genome-canvas';hero.append(canvas);const cue=document.createElement('p');cue.className='hero-interaction';cue.innerHTML='<b>MOVE</b> TO DISTORT THE SIGNAL · <b>CLICK</b> TO PULSE';hero.append(cue);const ctx=canvas.getContext('2d');let w,h,dpr,mouse={x:-999,y:-999},pulse=0,t=0;function size(){dpr=Math.min(devicePixelRatio,2);w=canvas.clientWidth;h=canvas.clientHeight;canvas.width=w*dpr;canvas.height=h*dpr;ctx.setTransform(dpr,0,0,dpr,0,0)}size();addEventListener('resize',size);hero.addEventListener('pointermove',e=>{const r=canvas.getBoundingClientRect();mouse.x=e.clientX-r.left;mouse.y=e.clientY-r.top});hero.addEventListener('pointerleave',()=>mouse.x=-999);hero.addEventListener('pointerdown',()=>pulse=1);function frame(){t+=.014;ctx.clearRect(0,0,w,h);const n=58,cx=w*.56,amp=w*.18,step=h/(n-1);for(let i=0;i<n;i++){const y=i*step+Math.sin(t+i*.2)*4;let x1=cx+Math.sin(i*.34+t)*amp,x2=cx-Math.sin(i*.34+t)*amp;const d1=Math.hypot(mouse.x-x1,mouse.y-y),d2=Math.hypot(mouse.x-x2,mouse.y-y);const warp=Math.max(0,1-Math.min(d1,d2)/180);x1+=(x1-mouse.x)*warp*.5;x2+=(x2-mouse.x)*warp*.5;ctx.strokeStyle=`rgba(214,170,105,${.18+warp*.35})`;ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(x1,y);ctx.lineTo(x2,y);ctx.stroke();for(const [x,c] of [[x1,'#d6aa69'],[x2,'#b95d48']]){ctx.fillStyle=c;ctx.beginPath();ctx.arc(x,y,2.5+warp*2,0,Math.PI*2);ctx.fill();if(i<n-1&&i%2===0){ctx.fillStyle='rgba(229,221,203,.14)';ctx.beginPath();ctx.arc((x1+x2)/2,y,1,0,7);ctx.fill()}}}if(pulse>0){ctx.strokeStyle=`rgba(229,221,203,${pulse*.6})`;ctx.lineWidth=2;ctx.beginPath();ctx.arc(mouse.x<0?cx:mouse.x,mouse.y<0?h/2:mouse.y,(1-pulse)*600,0,7);ctx.stroke();pulse-=.025}requestAnimationFrame(frame)}frame()});
