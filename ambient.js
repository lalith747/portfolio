document.addEventListener('DOMContentLoaded',()=>{const button=document.createElement('button');button.className='ambient-toggle';button.textContent='◌ SOUND: OFF';document.body.append(button);let ctx,master,playing=false,timer;function note(f,start,duration,type='triangle',volume=.11){const o=ctx.createOscillator(),g=ctx.createGain();o.type=type;o.frequency.setValueAtTime(f,start);g.gain.setValueAtTime(.0001,start);g.gain.exponentialRampToValueAtTime(volume,start+.025);g.gain.exponentialRampToValueAtTime(.0001,start+duration);o.connect(g).connect(master);o.start(start);o.stop(start+duration+.03)}function playLoop(){if(!playing)return;const s=ctx.currentTime+.04;[110,130.81,146.83,164.81,146.83,130.81,98,110].forEach((f,i)=>note(f,s+i*.3,.26,i%3?'triangle':'sawtooth',.12));[55,55,49,55].forEach((f,i)=>note(f,s+i*.6,.5,'sine',.09));timer=setTimeout(playLoop,2500)}button.addEventListener('click',async()=>{if(!ctx){const AC=window.AudioContext||window.webkitAudioContext;ctx=new AC();master=ctx.createGain();master.gain.value=.8;master.connect(ctx.destination)}playing=!playing;clearTimeout(timer);if(playing){await ctx.resume();button.classList.add('on');button.textContent='● SOUND: ON';note(220,ctx.currentTime,.18,'sine',.13);playLoop()}else{button.classList.remove('on');button.textContent='◌ SOUND: OFF'}})});
