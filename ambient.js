document.addEventListener('DOMContentLoaded',()=>{const b=document.createElement('button');b.className='ambient-toggle';b.textContent='◌ SOUND: OFF';document.body.append(b);let ctx,playing=false,timer;function phrase(){if(!playing)return;const notes=[110,123.47,146.83,98,110,164.81,130.81];const start=ctx.currentTime;notes.forEach((f,i)=>{const o=ctx.createOscillator(),g=ctx.createGain();o.type=i%3===0?'triangle':'sine';o.frequency.value=f;g.gain.setValueAtTime(.0001,start+i*.34);g.gain.exponentialRampToValueAtTime(.035,start+i*.34+.06);g.gain.exponentialRampToValueAtTime(.0001,start+i*.34+.48);o.connect(g).connect(ctx.destination);o.start(start+i*.34);o.stop(start+i*.34+.52)});timer=setTimeout(phrase,3600)}b.addEventListener('click',()=>{if(!ctx)ctx=new AudioContext();playing=!playing;b.classList.toggle('on',playing);b.textContent=playing?'SOUND: ON':'◌ SOUND: OFF';if(playing){ctx.resume();phrase()}else clearTimeout(timer)})});
